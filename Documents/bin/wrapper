#!/bin/sh
set -eu

basename="$(basename "$0")"
command="$(env -i which "$basename")" || { echo "$basename: not found"; exit 127; }
if alias="$(which "$basename-${1:-}")"
then
    shift
    "$alias" "$@"
else
    "$command" "$@"
fi
